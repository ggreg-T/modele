    1  mkdir testdir == creer un dossier
    2  ls = lister les elements d'un dossier
    3  pwd == indique le chemin
    4  cd (nom du dossier) == parcourir des dossier
    5  cd .. = remonter d'un dossier parent
    6  ls -l == affiche lplus d'info sur les dossier dans le dossier parent
    7  touch file.txt == creer un fichier
    8  less = regarder ce qu'il y a dans un fichier
    9  cat file.txt ==  afficher ce que dit un fichier txt 
   10   code file.txt == ouvrir visual code
   11   rm = efface un fichier
   12   history == montre l'historique du terminal
   14  man ls == explication de fonction 
   15  history > history_log.txt == creer un fichier repertoriant l'historique
   16  mv ( nom de fichier) (destination) = deplacer un ficier (sert aussi a renommer)
   17  grep = trouver une partie precise d'un fichier exemple => grep une phrase/mot textdir
   18  help = decrit une fonction
   19  nano file.txt =  ecrire, modifier unfichier
   20  ls -a = affiche les dossier cache (on peut faire les 2, ls -la)
   21  which ls = localise une commande
   22  locate fichier.txt = permet de localiser un fichier sur le dd
   23  cp nom fichier.txt = copie un fichier 
   24  sudo fonction nomfichier = permet d'executer certaine commande restreinte en administrateur, apres avoir entrer le mot de passe
 












